check:
  local_backup_{{ grains['fqdn'] }}:
    command: "PATH=$PATH:/usr/local/sbin KRB5CCNAME=/tmp/krb5cc_sensu_backupninja duplicity_salt.sh nagios 24"
    interval: 3600
    timeout: 300
    occurrences: 1
    subscribers:
    - {{ grains['fqdn']|replace('.', '-') }}
